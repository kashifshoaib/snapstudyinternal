AWSTemplateFormatVersion: '2010-09-09'
Description: 'API Gateway HTTPS proxy for SnapStudy Elastic Beanstalk backend'

Parameters:
  BackendUrl:
    Type: String
    Default: 'snapstudy-backend-env.eba-ygmxm24z.us-east-1.elasticbeanstalk.com'
    Description: 'Elastic Beanstalk environment URL (without http://)'
  
  StageName:
    Type: String
    Default: 'prod'
    Description: 'API Gateway stage name'

Resources:
  # API Gateway REST API
  SnapStudyAPI:
    Type: AWS::ApiGateway::RestApi
    Properties:
      Name: 'SnapStudy-Backend-HTTPS-Proxy'
      Description: 'HTTPS API Gateway proxy to Elastic Beanstalk backend'
      EndpointConfiguration:
        Types:
          - REGIONAL
      BinaryMediaTypes:
        - '*/*'  # Support file uploads and all content types

  # API Gateway Resource for proxy paths
  ProxyResource:
    Type: AWS::ApiGateway::Resource
    Properties:
      RestApiId: !Ref SnapStudyAPI
      ParentId: !GetAtt SnapStudyAPI.RootResourceId
      PathPart: '{proxy+}'

  # HTTP Integration for proxy resource
  ProxyIntegration:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref SnapStudyAPI
      ResourceId: !Ref ProxyResource
      HttpMethod: ANY
      AuthorizationType: NONE
      RequestParameters:
        method.request.path.proxy: true
        method.request.header.Origin: false
      Integration:
        Type: HTTP_PROXY
        IntegrationHttpMethod: ANY
        Uri: !Sub 'http://${BackendUrl}/{proxy}'
        RequestParameters:
          integration.request.path.proxy: 'method.request.path.proxy'
        PassthroughBehavior: WHEN_NO_MATCH

  # HTTP Integration for root resource
  RootIntegration:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref SnapStudyAPI
      ResourceId: !GetAtt SnapStudyAPI.RootResourceId
      HttpMethod: ANY
      AuthorizationType: NONE
      RequestParameters:
        method.request.header.Origin: false
      Integration:
        Type: HTTP_PROXY
        IntegrationHttpMethod: ANY
        Uri: !Sub 'http://${BackendUrl}/'
        PassthroughBehavior: WHEN_NO_MATCH

  # OPTIONS method for CORS preflight on proxy resource
  ProxyOptionsMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref SnapStudyAPI
      ResourceId: !Ref ProxyResource
      HttpMethod: OPTIONS
      AuthorizationType: NONE
      Integration:
        Type: MOCK
        IntegrationResponses:
          - StatusCode: 200
            ResponseParameters:
              method.response.header.Access-Control-Allow-Origin: "'*'"
              method.response.header.Access-Control-Allow-Headers: "'Content-Type,X-Amz-Date,Authorization,X-Api-Key,X-Amz-Security-Token,X-Requested-With'"
              method.response.header.Access-Control-Allow-Methods: "'GET,POST,PUT,DELETE,OPTIONS,PATCH'"
            ResponseTemplates:
              application/json: ''
        RequestTemplates:
          application/json: '{"statusCode": 200}'
      MethodResponses:
        - StatusCode: 200
          ResponseParameters:
            method.response.header.Access-Control-Allow-Origin: true
            method.response.header.Access-Control-Allow-Headers: true
            method.response.header.Access-Control-Allow-Methods: true

  # OPTIONS method for CORS preflight on root resource
  RootOptionsMethod:
    Type: AWS::ApiGateway::Method
    Properties:
      RestApiId: !Ref SnapStudyAPI
      ResourceId: !GetAtt SnapStudyAPI.RootResourceId
      HttpMethod: OPTIONS
      AuthorizationType: NONE
      Integration:
        Type: MOCK
        IntegrationResponses:
          - StatusCode: 200
            ResponseParameters:
              method.response.header.Access-Control-Allow-Origin: "'*'"
              method.response.header.Access-Control-Allow-Headers: "'Content-Type,X-Amz-Date,Authorization,X-Api-Key,X-Amz-Security-Token,X-Requested-With'"
              method.response.header.Access-Control-Allow-Methods: "'GET,POST,PUT,DELETE,OPTIONS,PATCH'"
            ResponseTemplates:
              application/json: ''
        RequestTemplates:
          application/json: '{"statusCode": 200}'
      MethodResponses:
        - StatusCode: 200
          ResponseParameters:
            method.response.header.Access-Control-Allow-Origin: true
            method.response.header.Access-Control-Allow-Headers: true
            method.response.header.Access-Control-Allow-Methods: true

  # API Gateway Deployment
  APIDeployment:
    Type: AWS::ApiGateway::Deployment
    DependsOn:
      - ProxyIntegration
      - RootIntegration
      - ProxyOptionsMethod
      - RootOptionsMethod
    Properties:
      RestApiId: !Ref SnapStudyAPI
      StageName: !Ref StageName

Outputs:
  ApiGatewayUrl:
    Description: 'HTTPS API Gateway URL for SnapStudy backend'
    Value: !Sub 'https://${SnapStudyAPI}.execute-api.${AWS::Region}.amazonaws.com/${StageName}'
    Export:
      Name: 'SnapStudy-ApiGatewayUrl'
  
  ApiGatewayId:
    Description: 'API Gateway ID'
    Value: !Ref SnapStudyAPI
    Export:
      Name: 'SnapStudy-ApiGatewayId'
  
  BackendProxyUrl:
    Description: 'Backend URL being proxied'
    Value: !Sub 'http://${BackendUrl}'
    Export:
      Name: 'SnapStudy-BackendUrl'